[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
# Keep CI focused on correctness-level issues for an existing codebase.
select = ["E9", "F63", "F7", "F82"]

[tool.pytest.ini_options]
testpaths = ["V3/tests"]
addopts = "-q"
